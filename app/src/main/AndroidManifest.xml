<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shopmark">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".registro_ventas_MainActivity"
            android:exported="false" />
        <activity
            android:name=".ventas.ventas_MainActivity"
            android:exported="false" />
        <activity
            android:name=".client_MainActivity"
            android:exported="false" />
        <activity
            android:name=".store_house_MainActivity"
            android:exported="false" />
        <activity
            android:name=".buy_MainActivity"
            android:exported="false" />
        <activity
            android:name=".menu_MainActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".register_MainActivity" -->
        <!-- android:exported="false" -->
        <!-- /> -->
        <activity
            android:name=".login_MainActivity"
            android:exported="false" /> <!-- AGREGADO -->
        <!-- CLIENTES -->
        <activity android:name=".cliente.register_MainActivity" />
        <activity android:name=".cliente.ClientesActivity" />
        <activity android:name=".cliente.CBuscasActivity" />
        <activity android:name=".cliente.menuClienteActivity" /> <!-- Productos -->
        <activity android:name=".producto.RegistroProducto" />
        <activity android:name=".producto.ProductosActivity" />
        <activity android:name=".producto.PBuscasActivity" />
        <activity android:name=".producto.menuProductoActivity" /> <!-- AGREGADO -->
        <activity
            android:name=".first_MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>